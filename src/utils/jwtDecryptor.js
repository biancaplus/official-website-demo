// import jwt from "jsonwebtoken";

// export default {
//   decrypt(token) {
//     try {
//       const decoded = jwt.verify(token, ""); // JWT_SECRET 是加密时设置的密钥
//       return decoded;
//     } catch (error) {
//       console.log("Error while decoding token", error);
//       throw new Error("Invalid Token");
//     }
//   }
// };
