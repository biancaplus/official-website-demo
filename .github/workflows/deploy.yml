name: deploy
on:
  push:
    branches: ["main"]

jobs:
  deploy_prod:
    runs-on: [self-hosted]
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4
      - run: |
          "/var/www/publish1.sh $PWD"
        shell: bash
